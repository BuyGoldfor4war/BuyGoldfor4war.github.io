// Escucha el evento "load" para que se ejecute la animación al cargar la página
window.addEventListener('load', function() {
    var h1 = document.querySelector('.blur-animation');
    h1.style.animationPlayState = 'running';
  });
  